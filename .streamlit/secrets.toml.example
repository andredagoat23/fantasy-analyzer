# Copy this file to `.streamlit/secrets.toml` and paste your real key.
# The real secrets.toml is gitignored — never commit your key.
# For the deployed app, paste the same line into Streamlit Cloud → app → Settings → Secrets.
ANTHROPIC_API_KEY = "sk-ant-..."
